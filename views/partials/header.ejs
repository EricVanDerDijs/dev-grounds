<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>dev_Ground</title>
  <link href="https://fonts.googleapis.com/css?family=Inconsolata:400,700&amp;subset=latin-ext" rel="stylesheet">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css" integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M" crossorigin="anonymous">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.10/css/solid.css" integrity="sha384-HTDlLIcgXajNzMJv5hiW5s2fwegQng6Hi+fN6t5VAcwO/9qbg2YEANIyKBlqLsiT" crossorigin="anonymous">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.10/css/fontawesome.css" integrity="sha384-8WwquHbb2jqa7gKWSoAwbJBV2Q+/rQRss9UXL5wlvXOZfSodONmVnifo/+5xJIWX" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
<nav class="navbar navbar-expand-md mb-4 navbar-dark bg-dark">
  <% var loginPage = ""; var devgroundPage = ""; var signupPage = ""; %>
  <%if (page) { %>
    <% if(page === "/login") {loginPage="active"} %>
    <% if(page === "/signup") {signupPage ="active"} %>
    <% if(page === "/devgrounds") {devgroundPage ="active"} %>
  <% } %>

      <a class="navbar-brand <%=devgroundPage%>" href="/devgrounds"><em><b>{</b> dev_Grounds<b>}</b></em></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav ml-auto">
         <% if (!currentUser) {%>
          <li class="nav-item">
            <a class="nav-link <%=loginPage%>" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%=signupPage%>" href="/signup">Sign Up</a>
          </li>
          <% } else { %>
          <li class="nav-item">
            <span class="nav-link disabled" id="userloged">
              <strong>Signed in as <%= currentUser.username %></strong>
            </span>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
          <% } %>
        </ul>
      </div>
    </nav>
  <div class="container">       
  <% flash.forEach( function(flashItem){ %>   <!--  Display every message on res.locals.flash -->
    <div class="alert alert-<%=flashItem.type%> alert-dismissible fade show" role="alert">
      <%= flashItem.message%>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
   <% flash.pop() }); %>   <!--  removes the flash message from template -->
  </div>
